meta {
  name: List Email Templates
  type: http
  seq: 21
}

get {
  url: {{emailApiBaseUrl}}/api/email-templates
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

docs {
  **GET** `/api/email-templates` — JWT com `tenantId` (qualquer utilizador autenticado do tenant).
}
