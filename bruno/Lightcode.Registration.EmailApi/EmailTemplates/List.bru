meta {
  name: List Email Templates
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/email-templates
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

docs {
  **GET** `/api/email-templates` — requer role `template-read` ou scope `email-admin`.
}
